[package]
name = "riscv_transpiler"
version.workspace = true
edition.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
common_constants = { workspace = true }
field = { workspace = true }
blake2s_u32 = { workspace = true }
ruint = { version = "1.16" }
worker = { workspace = true }
serde = { workspace = true }
seq-macro = { workspace = true }
inferno = { workspace = true, optional = true }
addr2line_new = { workspace = true, optional = true }
object = { workspace = true, optional = true }
serde-big-array = "*"
keccak = "*"

dynasmrt = { version = "5", optional = true }
capstone = {version = "0.13", optional = true }

[features]
jit = ["dynasmrt", "capstone"]
flamegraph = ["dep:inferno", "dep:addr2line_new", "dep:object"]
# Register-allocation experiment selector for the timestamp-tracking JIT:
#   default (off): 8 RISC-V values in host GPRs (a0..a4/a6/t3/a5), their timestamps
#                  live in XMM lanes (xmm6/7/13/14) via `pinsrq`.
#   ts_in_gpr     : 4 RISC-V values in host GPRs (a0..a3), their timestamps in the 4
#                  freed GPRs (r14/r15/rbx/rbp) via move-eliminated `mov`.
ts_in_gpr = []
# Draft experiment: replace the 3 per-cycle register-timestamp writes with ONE store
# into a (32 x 33 x 33) u64 array in MachineState, indexed by the instruction's
# (rs1, rs2, rd) triple (rs2=32 for loads, rd=32 for stores). Register timestamps are
# meant to be reconstructed later by scanning the array (not implemented yet — perf draft).
# Applies to the EAGER path only.
packed_ts = []
default = ["jit"]

[dev-dependencies]
test_utils = { workspace = true }
lib-rv32-asm = {git = "https://github.com/shamatar/lib-rv32.git" }
hex = "*"
serial_test = "*"
