# Keep agent-local state out of Git. This file must remain tracked so the rule
# survives clones; only gpu_work.md and shared bin tooling stay in the repository.
*
!.gitignore
!gpu_work.md
!bin/
!bin/**
