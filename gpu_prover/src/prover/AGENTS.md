# AGENTS.md

Files under `src/prover/` are governed by the full
[`gpu_scheduling_contract.md`](../../docs/gpu_scheduling_contract.md).

Read that document before editing anything in this tree. The contract, not
this pointer file, is the source of truth for stream ordering, callback-only
accessors, fork/join rules, and pool-backed allocation lifetimes.
