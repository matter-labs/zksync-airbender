// fwd-VM v2 interpreter translation unit (Task 8 fills in the kernel).
// For now this TU exists so the native build compiles `fwd_vm.cuh` and its
// ABI static asserts on every build.

#include "fwd_vm.cuh"
