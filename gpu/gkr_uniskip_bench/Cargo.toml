[package]
name = "gpu_gkr_uniskip_bench"
version.workspace = true
edition.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
keywords.workspace = true
categories.workspace = true
build = "build.rs"
links = "gpu_gkr_uniskip_bench_native"

[dependencies]
clap = { workspace = true }
era_cudart = "=0.156.0"
era_cudart_sys = "=0.156.0"
field = { workspace = true }

[dev-dependencies]
# Only for `force_serial_libtest!` at the crate root (gpu/AGENTS.md testing
# contract) — the production build must not pull gpu_core's `cs`/`fft` tree.
gpu_core = { workspace = true }

[build-dependencies]
gpu_native_build = { workspace = true }
