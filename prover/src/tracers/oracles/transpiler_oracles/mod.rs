// Oracles for mem and non-mem families are just the same as in the baseline
// case of unrolled circuits, so we need to only handle the case of delegaitons,
// and of unified cycle

pub mod delegation;
